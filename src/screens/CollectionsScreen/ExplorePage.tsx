/**
 * Holds just a way to create a call Collection page with needed arguments
 */

import React from "react";
import CollectionsScreen from ".";

export default function ExplorePage() {
	return <CollectionsScreen isCollection={false} />;
}
